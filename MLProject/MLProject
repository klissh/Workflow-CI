name: CreditScoringCI

conda_env: environment.yml

entry_points:
  main:
    parameters:
      data_path: {type: string, default: "dataset_preprocessing/creditcard_processed.csv"}
    command: "python modelling.py --data_path {data_path}"